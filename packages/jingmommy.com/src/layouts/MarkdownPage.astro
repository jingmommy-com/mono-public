---
import Layout from "./Page.astro"
import Markdown from "@/components/Markdown.astro"

const { frontmatter = {}, title: propTitle, color: propColor, theme: propTheme } = Astro.props
const title = frontmatter.title ?? propTitle ?? null
const color = frontmatter.color ?? propColor ?? null
const theme = frontmatter.theme ?? propTheme ?? 'base'
---

<Layout title={title} color={color} theme={theme}>
  <Markdown class="px-4 md:px-6 lg:px-8 max-w-full">
    <slot />
  </Markdown>
</Layout>
